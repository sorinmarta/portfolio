import React, { Component } from "react";
import Navigation from "../layout/Navigation";

export default class Portfolio extends Component {
    render() {
      return (
        <div className="portfolio-wrap">
          <Navigation left="home" right="about" />

          sss
        </div>
      );
    }
  }